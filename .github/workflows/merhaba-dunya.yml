name: Merhaba dünya
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
jobs:
  # This workflow contains a single job called "merhaba"
  merhaba_ubuntu:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    steps:
      - name: merhaba dünya çalıştır
        run: echo Merhaba, dünya! Ubuntu

  merhaba_windows:
    runs-on: windows-latest
    steps:
      - name: merhaba dünya çalıştır
        run: echo Merhaba, dünya! Windows
